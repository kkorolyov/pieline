import { sleep } from "./util";

export const register = async () => {
  await sleep(500);
  return "bogoToken";
};
export const authenticate = async () => {
  await sleep(500);
  return "bogoToken";
};
