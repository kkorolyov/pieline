syntax = "proto3";

package common;

// A unique identifier
message Uuid {
  // UUID value
  string value = 1;
}
// List of unique identifiers
message UuidList {
  // Identifiers
  repeated Uuid ids = 1;
}
