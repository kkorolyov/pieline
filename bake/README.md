# PieLine container image bakery

Scripts for packaging PieLine services as OCI images.

The root-level `Makefile` builds and cleans images for selected services.  
Use `make` or `make help` to view help.
